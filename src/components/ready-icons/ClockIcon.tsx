import React from 'react'
import Icon from '../atoms/Icon/Icon'
import ClockBlueIconSource from '../../assets/icons/clock-blue-icon.svg'

interface IProps {}

const ClockIcon = (props: IProps) => {
  return <Icon source={ClockBlueIconSource} />
}

export default ClockIcon
